Q1. Write a C program to find the largest and smallest digit in a given number.

#include <stdio.h>
int main(){
    int n;
    int max=0,min=9;
    scanf("%d",&n);

    while (n!=0){
        int last=n%10;

        if(last>max)
            max=last;
        if(last<min)
            min=last;
        n/=10;
    }
    printf("%d\n",max);
    printf("%d",min);
    return 0;
}
Q2. C Program to find greatest and smallest among three numbers

#include<stdio.h>

int main(){
    int a, b, c;
    scanf("%d %d %d", &a, &b, &c);

    // Finding Maximum
    if(a > b && a > c)
        printf("Max: %d\n", a);
    else if(b > a && b > c)
        printf("Max: %d\n", b);
    else
        printf("Max: %d\n", c);

    // Finding Minimum
    if(a < b && a < c)
        printf("Min: %d", a);
    else if(b < a && b < c)
        printf("Min: %d", b);
    else
        printf("Min: %d", c);

    return 0;
}


---

Q3. C Program to check whether a given number is odd or even

#include<stdio.h>

int main(){
    int n;
    scanf("%d", &n);

    if(n % 2 == 0)
        printf("Even");
    else
        printf("Odd");

    return 0;
}


---

Q4. C Program to check whether a number is positive, negative, or zero

#include<stdio.h>

int main(){
    int n;
    scanf("%d", &n);

    if(n > 0)
        printf("Positive");
    else if(n < 0)
        printf("Negative");
    else
        printf("Zero");

    return 0;
}


---

Q5. C Program to check whether a given character is uppercase, lowercase, digit, or special symbol

#include<stdio.h>

int main(){
    char ch;
    scanf("%c", &ch);

    if(ch >= 'A' && ch <= 'Z')
        printf("Uppercase Letter");
    else if(ch >= 'a' && ch <= 'z')
        printf("Lowercase Letter");
    else if(ch >= '0' && ch <= '9')
        printf("Digit");
    else
        printf("Special Symbol");

    return 0;
}